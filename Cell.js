//definition of a Cell
//stores reference to the html node representing a cell and the coordinates (x, y) of the cell
function Cell( htmlNode, x, y ) {
    this.htmlNode = htmlNode; 
    this.x = x;
    this.y = y;
}